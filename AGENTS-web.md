# AGENTS.md

本文件用于约束代理在本项目应用代码中的工作方式，重点说明长期有效的架构约束、常见误区和隐藏前提。  
不约束可由 lint、formatter、静态检查等工具自动处理的机械性格式问题。

如果你发现新的高频错误、易混淆点或反直觉约束，应提醒开发者，并建议补充到本文件中。

## 沟通要求

- 默认使用简体中文与用户沟通、解释思路、汇报计划、说明改动和总结结果。
- 代码、命令、路径、API 名称、类型名、类名、库名、配置项保持原文，不要翻译。
- 除非用户明确要求英文，否则不要切换为英文回复。

## 总原则

- 优先延续现有项目结构和实现方式，不为了“更现代”或“更通用”重写已有体系。
- 先复用项目内已有成熟实现，再决定扩展或新增。
- 先保证链路闭合、状态一致、可维护，再考虑抽象是否优雅。
- 同一项目中不要并存两套入口组织、请求封装、表单处理或状态管理模式。

## 分层规则

- 入口层或页面层负责取数、组装模块、连接导航上下文和页面级状态流转。
- 视图组件负责展示和局部交互；通用 UI 组件与业务组件分开。
- 复用逻辑模块只沉淀可复用逻辑，不为一次性逻辑强行抽象。
- `api` / `request` 层负责协议对接、通用错误处理、鉴权、重试、上传下载等请求职责。
- `utils` 只放纯函数和通用工具，不放明显带业务上下文的逻辑。
- 不要把协议判断、权限判断、平台差异判断散落在入口层和视图层。

## 状态规则

- 全局状态容器只放跨页面共享、跨模块复用、或刷新后仍需保留的业务状态。
- 弹窗开关、hover、局部 loading、局部筛选状态、一次性表单草稿等默认放本地状态。
- 状态容器应表达业务真相，不存纯展示状态和易失真的派生状态。
- 优先通过明确方法修改状态，不要从外部到处直接改内部结构。
- 持久化只给确实需要跨刷新的状态。

## 导航与权限

- 登录态校验、初始化链路、权限收敛优先放在导航入口、请求层和用户状态模块中统一处理。
- 导航元信息或页面配置优先作为页面标题、布局和权限辅助信息来源。
- 页面依赖地址参数、查询参数或上下文参数时，应显式监听变化并重拉数据，保证地址与页面状态一致。
- 不要在多个页面重复写同一套鉴权和权限判断。

## 表单、表格与 CRUD

- 表单校验规则集中定义，不要散落在视图定义、事件函数和工具中。
- 如果项目已有统一表单抽象、schema 校验或字段配置方案，继续沿用。
- 新增/编辑优先复用一套表单结构，不复制两套近似实现。
- 列表页优先保持稳定结构：筛选区、操作区、表格区、分页区、弹窗/抽屉区。
- 删除、批量操作、重置等危险行为必须有统一确认交互。
- 增删改后使用稳定刷新策略，不要混用局部补丁更新和整体刷新。

## 多端与环境差异

- 平台差异优先收敛到拦截器、平台工具、上传封装、导航工具或兼容层。
- 不要把大量环境判断散落在页面和业务组件中。
- 环境变量用于地址、开关和环境差异，不要绑定零散业务逻辑。

## 修改现有项目时

- 改动前先查找是否已有同类实现，再决定复用、扩展还是新增。
- 新建目录或引入抽象前，先确认它是否真的降低维护成本。
- 对现有项目做增强时，优先补齐缺失链路和收敛重复逻辑，不优先做大规模重构。
- 如果发现与直觉不一致的实现，先确认它是否是项目既有约束、历史兼容方案或被多处依赖的事实标准。
